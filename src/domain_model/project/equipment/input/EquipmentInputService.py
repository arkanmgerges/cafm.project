"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from typing import List, Tuple
from src.domain_model.project.equipment.input.EquipmentInput import EquipmentInput
from src.domain_model.project.equipment.input.EquipmentInputRepository import (
    EquipmentInputRepository,
)
from src.domain_model.resource.exception.EquipmentInputAlreadyExistException import (
    EquipmentInputAlreadyExistException,
)
from src.domain_model.resource.exception.EquipmentInputDoesNotExistException import (
    EquipmentInputDoesNotExistException,
)
from src.domain_model.token.TokenData import TokenData
from src.resource.logging.decorator import debugLogger


class EquipmentInputService:
    def __init__(self, repository: EquipmentInputRepository):
        self._repo = repository

    @debugLogger
    def createEquipmentInput(
        self, obj: EquipmentInput, objectOnly: bool = False, tokenData: TokenData = None
    ):
        if objectOnly:
            return (
                EquipmentInput.createFromObject(obj=obj, generateNewId=True)
                if obj.id() == ""
                else obj
            )
        else:
            obj = EquipmentInput.createFromObject(obj=obj, publishEvent=True)
            self._repo.save(obj=obj)
            return obj

    @debugLogger
    def deleteEquipmentInput(self, obj: EquipmentInput, tokenData: TokenData = None):
        obj.publishDelete()
        self._repo.deleteEquipmentInput(obj=obj)

    @debugLogger
    def updateEquipmentInput(
        self,
        oldObject: EquipmentInput,
        newObject: EquipmentInput,
        tokenData: TokenData = None,
    ):
        newObject.publishUpdate(oldObject)
        self._repo.save(obj=newObject)

    @debugLogger
    def bulkCreate(self, objList: List[EquipmentInput]):
        self._repo.bulkSave(objList=objList)
        for obj in objList:
            EquipmentInput.createFromObject(obj=obj, publishEvent=True)

    @debugLogger
    def bulkDelete(self, objList: List[EquipmentInput]):
        self._repo.bulkDelete(objList=objList)
        for obj in objList:
            obj.publishDelete()

    @debugLogger
    def bulkUpdate(self, objList: List[Tuple]):
        newObjList = list(map(lambda x: x[0], objList))
        self._repo.bulkSave(objList=newObjList)
        for obj in objList:
            newObj = obj[0]
            oldObj = obj[1]
            newObj.publishUpdate(oldObj)

    @debugLogger
    def equipmentInputs(
        self,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.equipmentInputs(
            tokenData=tokenData,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )

    @debugLogger
    def equipmentInputsByEquipmentId(
        self,
        tokenData: TokenData = None,
        equipmentId: str = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.equipmentInputsByEquipmentId(
            tokenData=tokenData,
            equipmentId=equipmentId,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )
