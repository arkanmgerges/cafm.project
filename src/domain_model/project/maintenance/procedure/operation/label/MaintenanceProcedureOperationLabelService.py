"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from typing import List, Tuple
from src.domain_model.project.maintenance.procedure.operation.label.MaintenanceProcedureOperationLabel import MaintenanceProcedureOperationLabel
from src.domain_model.project.maintenance.procedure.operation.label.MaintenanceProcedureOperationLabelRepository import MaintenanceProcedureOperationLabelRepository
from src.domain_model.token.TokenData import TokenData
from src.resource.logging.decorator import debugLogger


class MaintenanceProcedureOperationLabelService:
    def __init__(self, repository: MaintenanceProcedureOperationLabelRepository):
        self._repo = repository

    @debugLogger
    def createMaintenanceProcedureOperationLabel(self, obj: MaintenanceProcedureOperationLabel, objectOnly: bool = False, tokenData: TokenData = None):
        if objectOnly:
            return MaintenanceProcedureOperationLabel.createFromObject(obj=obj, generateNewId=True) if obj.id() == '' else obj
        else:
            obj: MaintenanceProcedureOperationLabel = MaintenanceProcedureOperationLabel.createFromObject(obj=obj, publishEvent=True)
            self._repo.save(obj=obj)
            return obj

    @debugLogger
    def deleteMaintenanceProcedureOperationLabel(self, obj: MaintenanceProcedureOperationLabel, tokenData: TokenData = None):
        obj.publishDelete()
        self._repo.deleteMaintenanceProcedureOperationLabel(obj=obj)

    @debugLogger
    def updateMaintenanceProcedureOperationLabel(self, oldObject: MaintenanceProcedureOperationLabel, newObject: MaintenanceProcedureOperationLabel, tokenData: TokenData = None):
        newObject.publishUpdate(oldObject)
        self._repo.save(obj=newObject)

    @debugLogger
    def bulkCreate(self, objList: List[MaintenanceProcedureOperationLabel]):
        self._repo.bulkSave(objList=objList)
        for obj in objList:
            MaintenanceProcedureOperationLabel.createFromObject(obj=obj, publishEvent=True)

    @debugLogger
    def bulkDelete(self, objList: List[MaintenanceProcedureOperationLabel]):
        self._repo.bulkDelete(objList=objList)
        for obj in objList:
            obj.publishDelete()

    @debugLogger
    def bulkUpdate(self, objList: List[Tuple]):
        newObjList = list(map(lambda x: x[0], objList))
        self._repo.bulkSave(objList=newObjList)
        for obj in objList:
            newObj = obj[0]
            oldObj = obj[1]
            newObj.publishUpdate(oldObj)

    @debugLogger
    def maintenanceProcedureOperationLabels(self, tokenData: TokenData = None, resultFrom: int = 0, resultSize: int = 100,
                      order: List[dict] = None):
        return self._repo.maintenanceProcedureOperationLabels(tokenData=tokenData, resultFrom=resultFrom, resultSize=resultSize, order=order)
