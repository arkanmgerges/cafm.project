"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from abc import ABC, abstractmethod
from typing import List
from src.domain_model.project.standard_equipment.StandardEquipment import (
    StandardEquipment,
)
from src.domain_model.token.TokenData import TokenData


class StandardEquipmentRepository(ABC):
    @abstractmethod
    def bulkSave(self, objList: List[StandardEquipment], tokenData: TokenData):
        """Bulk save standard equipment list

        Args:
            objList (List[StandardEquipment]): The standard equipment list that needs to be saved
            tokenData (TokenData): Token data that has info about the token

        """

    @abstractmethod
    def bulkDelete(self, objList: List[StandardEquipment], tokenData: TokenData):
        """Bulk delete standard equipment list

        Args:
            objList (List[StandardEquipment]): The standard equipment list that needs to be deleted
            tokenData (TokenData): Token data that has info about the token

        """

    @abstractmethod
    def save(self, obj: StandardEquipment, tokenData: TokenData):
        """Save standard equipment

        Args:
            obj (StandardEquipment): The standard equipment that needs to be saved
            tokenData (TokenData): Token data that has info about the token

        """

    @abstractmethod
    def deleteStandardEquipment(
        self, obj: StandardEquipment, tokenData: TokenData
    ) -> None:
        """Delete a standard equipment

        Args:
            obj (StandardEquipment): The standard equipment that needs to be deleted
            tokenData (TokenData): Token data used for deleting the standard equipment

        :raises:
            `ObjectCouldNotNotBeDeletedException
            <src.domain_model.resource.exception.ObjectCouldNotNotBeDeletedException>`
            Raise an exception if the standard equipment could not be deleted
        """

    @abstractmethod
    def standardEquipmentById(self, id: str) -> StandardEquipment:
        """Get standard equipment by id

        Args:
            id (str): The id of the standard equipment

        Returns:
            StandardEquipment: standard equipment object

        :raises:
            `StandardEquipmentDoesNotExistException <src.domain_model.resource.exception.StandardEquipmentDoesNotExistException>`
            Raise an exception if the standard equipment does not exist
        """

    @abstractmethod
    def standardEquipments(
        self,
        tokenData: TokenData,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ) -> dict:
        """Get list of standard equipments based on the owned roles that the user has

        Args:
            tokenData (TokenData): A token data object
            resultFrom (int): The start offset of the result item
            resultSize (int): The size of the items in the result
            order (List[dict]): A list of order e.g. [{'orderBy': 'name', 'direction': 'asc'},
                                {'orderBy': 'quantity', 'direction': 'desc'}]

        Returns:
            dict: A dict that has {"items": [], "totalItemCount": 0}
        """
