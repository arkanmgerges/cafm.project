"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from typing import List
from src.domain_model.project.standard_equipment.standard_category.StandardEquipmentCategory import (
    StandardEquipmentCategory,
)
from src.domain_model.project.standard_equipment.standard_category.StandardEquipmentCategoryRepository import (
    StandardEquipmentCategoryRepository,
)
from src.domain_model.token.TokenData import TokenData
from src.resource.logging.decorator import debugLogger


class StandardEquipmentCategoryService:
    def __init__(self, repository: StandardEquipmentCategoryRepository):
        self._repo = repository

    @debugLogger
    def createStandardEquipmentCategory(
        self,
        obj: StandardEquipmentCategory,
        objectOnly: bool = False,
        tokenData: TokenData = None,
    ):
        if objectOnly:
            return (
                StandardEquipmentCategory.createFromObject(obj=obj, generateNewId=True)
                if obj.id() == ""
                else obj
            )
        else:
            obj = StandardEquipmentCategory.createFromObject(obj=obj, publishEvent=True)
            self._repo.save(obj=obj)
            return obj

    @debugLogger
    def deleteStandardEquipmentCategory(
        self, obj: StandardEquipmentCategory, tokenData: TokenData = None
    ):
        obj.publishDelete()
        self._repo.deleteStandardEquipmentCategory(obj=obj)

    @debugLogger
    def updateStandardEquipmentCategory(
        self,
        oldObject: StandardEquipmentCategory,
        newObject: StandardEquipmentCategory,
        tokenData: TokenData = None,
    ):
        newObject.publishUpdate(oldObject)
        self._repo.save(obj=newObject)

    @debugLogger
    def standardEquipmentCategories(
        self,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.standardEquipmentCategories(
            tokenData=tokenData,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )
