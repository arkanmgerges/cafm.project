"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from typing import List, Tuple
from src.domain_model.project.standard_equipment.standard_project.standard_category.StandardEquipmentProjectCategory import (
    StandardEquipmentProjectCategory,
)
from src.domain_model.project.standard_equipment.standard_project.standard_category.StandardEquipmentProjectCategoryRepository import (
    StandardEquipmentProjectCategoryRepository,
)
from src.domain_model.token.TokenData import TokenData
from src.resource.logging.decorator import debugLogger


class StandardEquipmentProjectCategoryService:
    def __init__(self, repository: StandardEquipmentProjectCategoryRepository):
        self._repo = repository

    @debugLogger
    def createStandardEquipmentProjectCategory(
        self,
        obj: StandardEquipmentProjectCategory,
        objectOnly: bool = False,
        tokenData: TokenData = None,
    ):
        if objectOnly:
            return (
                StandardEquipmentProjectCategory.createFromObject(
                    obj=obj, generateNewId=True
                )
                if obj.id() == ""
                else obj
            )
        else:
            obj: StandardEquipmentProjectCategory = (
                StandardEquipmentProjectCategory.createFromObject(
                    obj=obj, publishEvent=True
                )
            )
            self._repo.save(obj=obj)
            return obj

    @debugLogger
    def deleteStandardEquipmentProjectCategory(
        self, obj: StandardEquipmentProjectCategory, tokenData: TokenData = None
    ):
        obj.publishDelete()
        self._repo.deleteStandardEquipmentProjectCategory(obj=obj)

    @debugLogger
    def updateStandardEquipmentProjectCategory(
        self,
        oldObject: StandardEquipmentProjectCategory,
        newObject: StandardEquipmentProjectCategory,
        tokenData: TokenData = None,
    ):
        newObject.publishUpdate(oldObject)
        self._repo.save(obj=newObject)

    @debugLogger
    def bulkCreate(self, objList: List[StandardEquipmentProjectCategory]):
        self._repo.bulkSave(objList=objList)
        for obj in objList:
            StandardEquipmentProjectCategory.createFromObject(
                obj=obj, publishEvent=True
            )

    @debugLogger
    def bulkDelete(self, objList: List[StandardEquipmentProjectCategory]):
        self._repo.bulkDelete(objList=objList)
        for obj in objList:
            obj.publishDelete()

    @debugLogger
    def bulkUpdate(self, objList: List[Tuple]):
        newObjList = list(map(lambda x: x[0], objList))
        self._repo.bulkSave(objList=newObjList)
        for obj in objList:
            newObj = obj[0]
            oldObj = obj[1]
            newObj.publishUpdate(oldObj)

    @debugLogger
    def standardEquipmentProjectCategories(
        self,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.standardEquipmentProjectCategories(
            tokenData=tokenData,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )

    @debugLogger
    def standardEquipmentProjectCategoriesyOrganizationId(
        self,
        organizationId: str = None,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.standardEquipmentProjectCategoriesByOrganizationId(
            tokenData=tokenData,
            organizationId=organizationId,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )
