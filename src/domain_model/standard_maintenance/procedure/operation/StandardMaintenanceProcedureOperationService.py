"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""
from typing import List, Tuple

from src.domain_model.standard_maintenance.procedure.operation.StandardMaintenanceProcedureOperation import (
    StandardMaintenanceProcedureOperation,
)
from src.domain_model.standard_maintenance.procedure.operation.StandardMaintenanceProcedureOperationRepository import (
    StandardMaintenanceProcedureOperationRepository,
)
from src.domain_model.token.TokenData import TokenData
from src.resource.logging.decorator import debugLogger


class StandardMaintenanceProcedureOperationService:
    def __init__(self, repository: StandardMaintenanceProcedureOperationRepository):
        self._repo = repository

    @debugLogger
    def createStandardMaintenanceProcedureOperation(
        self,
        obj: StandardMaintenanceProcedureOperation,
        objectOnly: bool = False,
        tokenData: TokenData = None,
    ):
        if objectOnly:
            return (
                StandardMaintenanceProcedureOperation.createFromObject(
                    obj=obj, generateNewId=True
                )
                if obj.id() == ""
                else obj
            )
        else:
            obj = StandardMaintenanceProcedureOperation.createFromObject(
                obj=obj, publishEvent=True
            )
            self._repo.save(obj=obj)
            return obj

    @debugLogger
    def deleteStandardMaintenanceProcedureOperation(
        self, obj: StandardMaintenanceProcedureOperation, tokenData: TokenData = None
    ):
        obj.publishDelete()
        self._repo.deleteStandardMaintenanceProcedureOperation(obj=obj)

    @debugLogger
    def updateStandardMaintenanceProcedureOperation(
        self,
        oldObject: StandardMaintenanceProcedureOperation,
        newObject: StandardMaintenanceProcedureOperation,
        tokenData: TokenData = None,
    ):
        newObject.publishUpdate(oldObject)
        self._repo.save(obj=newObject)

    @debugLogger
    def bulkCreate(self, objList: List[StandardMaintenanceProcedureOperation]):
        self._repo.bulkSave(objList=objList)
        for obj in objList:
            StandardMaintenanceProcedureOperation.createFromObject(obj=obj, publishEvent=True)

    @debugLogger
    def bulkDelete(self, objList: List[StandardMaintenanceProcedureOperation]):
        self._repo.bulkDelete(objList=objList)
        for obj in objList:
            obj.publishDelete()

    @debugLogger
    def bulkUpdate(self, objList: List[Tuple]):
        newObjList = list(map(lambda x: x[0], objList))
        self._repo.bulkSave(objList=newObjList)
        for obj in objList:
            newObj = obj[0]
            oldObj = obj[1]
            newObj.publishUpdate(oldObj)

    @debugLogger
    def standardMaintenanceProcedureOperations(
        self,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.standardMaintenanceProcedureOperations(
            tokenData=tokenData,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )

    @debugLogger
    def standardMaintenanceProcedureOperationsByStandardMaintenanceProcedureId(
        self,
        standardMaintenanceProcedureId: str = None,
        tokenData: TokenData = None,
        resultFrom: int = 0,
        resultSize: int = 100,
        order: List[dict] = None,
    ):
        return self._repo.standardMaintenanceProcedureOperationsByStandardMaintenanceProcedureId(
            tokenData=tokenData,
            standardMaintenanceProcedureId=standardMaintenanceProcedureId,
            resultFrom=resultFrom,
            resultSize=resultSize,
            order=order,
        )
