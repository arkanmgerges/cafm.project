"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""


import json

import src.port_adapter.AppDi as AppDi
from src.application.StandardEquipmentCategoryGroupApplicationService import StandardEquipmentCategoryGroupApplicationService
from src.domain_model.resource.exception.UnAuthorizedException import UnAuthorizedException
from src.port_adapter.messaging.listener.CommandConstant import CommonCommandConstant
from src.port_adapter.messaging.listener.common.handler.Handler import Handler
from src.resource.common.DateTimeHelper import DateTimeHelper
from src.resource.logging.logger import logger


class UpdateStandardEquipmentCategoryGroupHandler(Handler):

    def __init__(self):
        self._commandConstant = CommonCommandConstant.UPDATE_STANDARD_EQUIPMENT_CATEGORY_GROUP

    def canHandle(self, name: str) -> bool:
        return name == self._commandConstant.value

    def handleCommand(self, messageData: dict) -> dict:
        name = messageData['name']
        data = messageData['data']
        metadata = messageData['metadata']

        logger.debug(
            f'[{UpdateStandardEquipmentCategoryGroupHandler.handleCommand.__qualname__}] - received args:\ntype(name): {type(name)}, name: {name}\ntype(data): {type(data)}, data: {data}\ntype(metadata): {type(metadata)}, metadata: {metadata}')

        appService: StandardEquipmentCategoryGroupApplicationService = AppDi.instance.get(StandardEquipmentCategoryGroupApplicationService)
        dataDict = json.loads(data)
        metadataDict = json.loads(metadata)

        if 'token' not in metadataDict:
            raise UnAuthorizedException()

        id = dataDict['standard_equipment_category_group_id'] if 'standard_equipment_category_group_id' in dataDict else None
        appService.updateStandardEquipmentCategoryGroup(id=id, 
			name=dataDict["name"] if "name" in dataDict else None,
			standardEquipmentCategoryId=dataDict["standard_equipment_category_id"] if "standard_equipment_category_id" in dataDict else None, token=metadataDict['token'])
        return {'name': self._commandConstant.value, 'created_on': DateTimeHelper.utcNow(),
                'data': dataDict,
                'metadata': metadataDict}
