"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""



from datetime import datetime

from sqlalchemy import Column, Integer, String, Boolean, ForeignKey, Float
from sqlalchemy.sql.sqltypes import DateTime
from sqlalchemy.orm import relationship
import src.port_adapter.AppDi as AppDi

Base = AppDi.instance.get(AppDi.DbBase)


class MaintenanceProcedureOperationParameter(Base):
    __tablename__ = 'maintenance_procedure_operation_parameter'
    id = Column('id', String(40), primary_key=True)
    name = Column('name', String(255))
    unitId = Column('unit_id', String(40), ForeignKey('unit.id', ondelete='CASCADE'), nullable=False)
    maintenanceProcedureOperationId = Column('maintenance_procedure_operation_id', String(40), ForeignKey('maintenance_procedure_operation.id', ondelete='CASCADE'), nullable=False)
    minValue = Column('min_value', Float)
    maxValue = Column('max_value', Float)
    createdAt = Column('created_at', DateTime, nullable=True, default=datetime.utcnow())
    modifiedAt = Column('modified_at', DateTime, nullable=True, onupdate=datetime.utcnow())

    # Relationship
    operation = relationship(
        "MaintenanceProcedureOperation",
        back_populates="parameters", lazy='joined')

    def __repr__(self):
        return f"[Repo DB Model] MaintenanceProcedureOperationParameter(id='{self.id}', name='{self.name}', unitId='{self.unitId}', maintenanceProcedureOperationId='{self.maintenanceProcedureOperationId}', minValue='{self.minValue}', maxValue='{self.maxValue}', )"
