"""
The file is generated by a scaffold script
@author: Arkan M. Gerges<arkan.m.gerges@gmail.com>
"""

import pytest

from src.domain_model.event.DomainPublishedEvents import DomainPublishedEvents
from src.domain_model.role.Role import Role


def setup_function(function):
    DomainPublishedEvents.cleanup()


def test_create_object():
    # Act
    obj = _create_object()
    # Assert
    assert isinstance(obj, Role)

def test_name():
    # Act
    obj = _create_object()
    # Assert
    assert obj.name() == 'name'
def test_title():
    # Act
    obj = _create_object()
    # Assert
    assert obj.title() == 'title'


def test_create_from_object():
    # Act
    obj = _create_object()
    obj2 = Role.createFromObject(obj=obj)
    # Assert
    assert obj == obj2


def test_toMap():
    # Arrange
    obj = _create_object(id='1',
        name = 'name',
        title = 'title',
    )
    currentMap = {'role_id': '1',
        'name': 'name',
        'title': 'title',
    }
    # Act
    objectMap = obj.toMap()

    # Assert
    assert objectMap == currentMap
    assert len(objectMap.keys()) == len(currentMap.keys())


def _create_object(id: str = None, name: str = None, title: str = None, skipValidation: bool = False):
    id = '1' if id is None else id
    name = 'name' if name is None else name
    title = 'title' if title is None else title

    return Role.createFrom(id=id, 
			name=name,
			title=title, skipValidation=skipValidation)
